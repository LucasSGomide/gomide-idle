# Design rules

UI/UX guidelines for the game's interface. `project.yml` points every
`**Design**` bullet in a roadmap item at this file.

Nothing lives here yet — every rule this doc used to hold was game-balance and
economy tuning advice, not UI/UX guidance. That content moved to
[`alpha.md`](../alpha.md)'s Functional Requirements and Notes tables. Add real
UI/UX rules here as they're decided.
