{
  "name": "gomide-idle",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "lint": "oxlint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build",
    "depcruise": "pnpm --filter @gomide/api depcruise",
    "generate": "pnpm --filter @gomide/api generate"
  },
  "devDependencies": {
    "oxlint": "catalog:",
    "typescript": "catalog:"
  }
}
