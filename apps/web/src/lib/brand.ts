// naming.md rule 16: the game is _Tormented Path_; its live content is one
// season at a time, currently _Mortal Ways_. Both are proper nouns — authored
// in English, shown identically in both languages, never translation keys
// (naming.md rule 13, architecture-api.md rule 87).
//
// This is the one place either string is written. Nothing else in the app
// spells "Tormented Path" or "Mortal Ways" as a literal, so a later major
// version swaps the season in one edit rather than a search across the tree.
export const GAME_NAME = 'Tormented Path';
export const ACTIVE_SEASON = 'Mortal Ways';
