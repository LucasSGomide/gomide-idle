// Better Auth's generated tables (auth.md rule 5), re-exported the way
// system's schema folder does. The file beside this one is written by
// `make api-auth-schema` and never hand-edited.
export * from './auth.schema.js';
