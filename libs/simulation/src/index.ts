// libs/simulation is the home for the deterministic combat code (FR.9.5).
// It is placed here empty; the Automatic combat roadmap item populates it.
// stack-api.md rule 7 / architecture-api.md rule 9: this package depends on
// nothing, which is what makes its determinism rules enforced rather than
// remembered.
export {};
